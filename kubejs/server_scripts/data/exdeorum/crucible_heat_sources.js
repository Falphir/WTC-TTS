ServerEvents.recipes(event => {

    // ExDeorumCrucibleHeatSource(event, block, heatValue)

    ExDeorumCrucibleHeatSource(event, "minecraft:fire",       5)
    ExDeorumCrucibleHeatSource(event, "minecraft:soul_fire",  7)
    ExDeorumCrucibleHeatSource(event, "minecraft:magma_block", 3)
    ExDeorumCrucibleHeatSource(event, "minecraft:campfire",   2)

})
